[project]
name = "pseudohcm"
version = "0.1.0"
description = "Test harness for AIeConA HR. Never ships."
requires-python = ">=3.11"
dependencies = ["aiecona-adapter-contract"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
