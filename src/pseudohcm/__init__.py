"""PseudoHCM test harness. Never ships.

Every record emitted from this package carries the reserved synthetic marker and
identifier namespace defined in the contract. Production ingestion hard-rejects both.
"""

__version__ = "0.1.0"
